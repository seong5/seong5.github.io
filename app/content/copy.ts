/* ──────────────────────────────────────────────────────────
   히어로 카피 단일 출처.

   Hero(app/components/Hero.tsx)와 OG 이미지(app/og.png/route.tsx)가
   같은 문장을 쓴다. 예전 og-image.png는 손으로 만든 PNG라 문구가 따로 놀았고,
   히어로만 고치면 공유 링크에는 옛 문구가 남았다. 그래서 여기로 모은다.

   HEADLINE은 두 행이 각각 .rise-mask 안에서 올라온다 — 한 문장을 두 줄로
   끊은 형태라 line1 끝에 문장부호를 두지 않는다.
   ────────────────────────────────────────────────────────── */

export const HEADLINE = {
  line1: 'From Idea',
  /** 라임이 얹히는 행 — "결과"에 색이 떨어진다 */
  line2: 'to Production.',
} as const;

/**
 * 히어로 리드 · OG 본문이 공유한다.
 * HEADLINE과 같이 한 문장을 두 행으로 끊은 형태라 line1 끝에 문장부호를 두지 않는다.
 * 문자열에 <br />을 넣지 않는 이유 — OG는 Satori라 JSX 문자열이 먹지 않는다.
 */
export const LEAD = {
  line1: '빠르게 배우고 적용하며 끝까지 파고들어',
  line2: '문제를 확실한 성과로 만듭니다.',
} as const;

/** 각각 독립 <p>다. <br />로 고정하면 뷰포트마다 조각 줄이 생긴다. */
export const INTRO = [
  '개발하며 마주한 불편함과 문제점을 그냥 넘기지 않고 하나의 서비스나 기능으로 풀어내며 성취감을 느끼는 개발자입니다.',
  '매사에 두려움보다는 호기심과 도전으로 접근하여 빠르게 경험하고 학습해 나가는 것을 좋아합니다.',
  '사용자 피드백과 문제 지점을 파고들어 눈에 보이는 성과로 전환하여 사용할수록 더욱 나아지는 서비스를 만드는 데 집중합니다.',
] as const;

/** 히어로 상태 줄 */
export const KICKER = 'SOFTWARE ENGINEER · FRONTEND DEVELOPER';
